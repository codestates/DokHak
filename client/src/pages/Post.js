import React from 'react';

import { Main } from './styles';

const Post = () => {
  return (
    <Main>
      {/* map으로 Card */}
      {/* Card onClick -> PostDetail */}
    </Main>
  );
};

export default Post;
